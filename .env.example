GEMINI_API_KEY=your_gemini_api_key_here

# Stripe billing — leave blank to keep the paywall OFF (app runs fully free).
# Set all three to enable the $5/mo subscription paywall.
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
# Public base URL used for Stripe redirect URLs (set to your custom domain later).
PUBLIC_BASE_URL=https://tonetitor-346314536777.asia-southeast1.run.app
